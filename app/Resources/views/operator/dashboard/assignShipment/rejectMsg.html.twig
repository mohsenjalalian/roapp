{% extends ':operator/dashboard:layout.html.twig' %}

{% block main %}
    <div class="ui info message">
        <h4>رد درخواست!!!</h4>
        <p>درخواست شما توسط راننده ی مورد نظر رد شده است.برای انتصاب مجدد سفارش به راننده ی دیگر به لینک زیر مراجعه فرمایید. </p>
        <a href="{{ path("app_operator_dashboard_assign_shipment_driver_assign") }}/{{ shipmentId }}"><button>انتصاب مجدد</button></a>
        <a href="{{ path("app_operator_dashboard_assign_shipment_index") }}"><button>بازگشت</button></a>
    </div>
{% endblock %}