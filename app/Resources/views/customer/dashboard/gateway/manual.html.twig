{% extends ':customer/dashboard:layout.html.twig' %}

{% block main %}
    <div class="panel-heading">
    <h2>ثبت اطلاعات کارت</h2>
    </div>
<div class="panel-body">
    {{ form_start(form)}}
    {{ form_widget(form) }}
    <input class="btn" type="submit" style="margin-top: 10px " value="ثبت اطلاعات" />
    {{ form_end(form) }}
</div>
{% endblock %}